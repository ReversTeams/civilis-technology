name: Publish Minecraft Mod

on:
  push:
    tags:
      - "*"

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: 'gradle'

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Build Mod
        run: ./gradlew build

      - name: Publish mod files
        uses: tristankechlo/publish-mc-mod@v2.0.0
        with:
          mc-version: "1.21.1"
          mod-version: "1.0.1"
          curseforge-id: 1499755
          modrinth-id: "TZBlTKuM"
          version-range: "[1.21.1,1.21.2)"
          version-type: "release"
          loaders: "neoforge"
          changelog: "- Added compatibility with Immersive Engineering"

          curseforge-token: ${{ secrets.CURSEFORGE_TOKEN }}
          modrinth-token: ${{ secrets.MODRINTH_TOKEN }}
          neoforge-glob: ./build/libs/*-universal-!(*-dev|*-sources|*-javadoc).jar